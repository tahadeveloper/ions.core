<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Ions\Support\DB;

/**
 * Schema for the notifications 'database' channel.
 *
 * Copy this into the host's {src|app}/Database migrations directory (dropping
 * the .stub extension) and run `ions migrate` to create the table the
 * DatabaseChannel inserts into. Rename the table here AND set
 * config('notifications.table') if you want a non-default name.
 */
return new class () extends Migration {
    public function up(): void
    {
        $schema = DB::connection()->getSchemaBuilder();

        $schema->create('notifications', function (Blueprint $table) {
            $table->uuid('id')->primary();
            $table->string('notifiable_type');
            $table->string('notifiable_id');
            $table->string('type');
            $table->text('data');
            $table->timestamp('read_at')->nullable();
            $table->timestamp('created_at')->nullable();
            $table->index(['notifiable_type', 'notifiable_id']);
        });
    }

    public function down(): void
    {
        $schema = DB::connection()->getSchemaBuilder();
        $schema->dropIfExists('notifications');
    }
};
