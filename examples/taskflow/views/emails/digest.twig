<!DOCTYPE html>
<html lang="en">
<body style="font-family: ui-sans-serif, system-ui, sans-serif; color: #1a1a1a;">
    <h1>Your weekly {{ app_name }} digest</h1>
    <p>Hi {{ name }},</p>
    <p>You have <strong>{{ open_tasks }}</strong> open task{{ open_tasks == 1 ? '' : 's' }} this week.</p>
    <p>See you in {{ app_name }}.</p>
</body>
</html>
